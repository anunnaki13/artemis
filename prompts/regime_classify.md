Classify the market regime as one of:

- trending
- ranging
- choppy
- extreme

Use only supplied indicators and recent market data. Return structured JSON with `regime`, `confidence`, and `reasons`.

