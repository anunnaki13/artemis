import { LoginForm } from "./login-form";

export default function LoginPage() {
  return (
    <main className="terminal-grid grid min-h-screen place-items-center bg-base px-4 py-8">
      <LoginForm />
    </main>
  );
}
